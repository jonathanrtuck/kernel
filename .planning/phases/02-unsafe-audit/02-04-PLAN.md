---
phase: 02-unsafe-audit
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - .planning/phases/02-unsafe-audit/AUDIT-REPORT.md
autonomous: true
requirements: [SAFE-06]

must_haves:
  truths:
    - "A per-file audit record exists for every file in frame/"
    - "Each file is documented as clean, fixed, or known-risk"
    - "The total gap closure count is documented"
    - "Any open-bugs found during audit are listed with status"
  artifacts:
    - path: ".planning/phases/02-unsafe-audit/AUDIT-REPORT.md"
      provides: "Complete per-file audit status document"
      contains: "Status:"
  key_links:
    - from: ".planning/phases/02-unsafe-audit/AUDIT-REPORT.md"
      to: "src/frame/"
      via: "References every file audited"
      pattern: "frame/"
---

<objective>
Create the per-file audit report documenting the status of every file in frame/ (SAFE-06).

Purpose: The audit report is the deliverable that makes the audit verifiable and repeatable. It records what was checked, what was fixed, and what risks remain. Future contributors can consult it before modifying unsafe code.

Output: A markdown document with per-file status (clean / fixed / known-risk) and summary statistics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unsafe-audit/02-01-SUMMARY.md
@.planning/phases/02-unsafe-audit/02-02-SUMMARY.md
@.planning/phases/02-unsafe-audit/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate per-file audit report from plan summaries and codebase verification</name>
  <files>.planning/phases/02-unsafe-audit/AUDIT-REPORT.md</files>
  <read_first>
    .planning/phases/02-unsafe-audit/02-01-SUMMARY.md (cores.rs + fields.rs audit results)
    .planning/phases/02-unsafe-audit/02-02-SUMMARY.md (mapping.rs + mmu.rs + boot.rs + slab.rs results)
    .planning/phases/02-unsafe-audit/02-03-SUMMARY.md (lock.rs + capabilities.rs + mod.rs + exception.rs + arch files results)
  </read_first>
  <action>
    1. Read all three plan summaries to collect:
       - Which files were audited
       - How many SAFETY comments were added/corrected per file
       - Any open-bugs or known-risk items discovered
       - Any asm options that were corrected

    2. Run verification counts on every file in frame/:
       ```
       for each file in src/frame/**/*.rs:
         count `unsafe {` blocks
         count `// SAFETY:` comments
         compute gap (should be 0)
       ```

    3. Create `.planning/phases/02-unsafe-audit/AUDIT-REPORT.md` with this structure:

       ```markdown
       # Unsafe Audit Report — frame/

       **Date:** [current date]
       **Auditor:** Claude (automated)
       **Scope:** All .rs files in src/frame/ and its subdirectories

       ## Summary

       | Metric | Before | After |
       |--------|--------|-------|
       | Total unsafe blocks | ~206 | [exact count] |
       | SAFETY comments | ~226 | [exact count] |
       | Gap (blocks without comments) | ~37 | 0 |
       | ASM options corrected | — | [count] |
       | Open bugs found | — | [count] |

       ## Per-File Status

       | File | unsafe | SAFETY | Status | Notes |
       |------|--------|--------|--------|-------|
       | cores.rs | N | N | clean/fixed | [what was done] |
       | fields.rs | N | N | clean/fixed | [what was done] |
       | ... (all files) ... |

       ## ASM Options Audit (SAFE-04)

       | File | Instruction | Old options | New options | Justification |
       |------|-------------|------------|-------------|---------------|
       | sysreg.rs | mrs MPIDR_EL1 | nomem | nomem | ARM ARM: immutable |
       | ... |

       ## Speculation Barriers (SAFE-05)

       | File | Function | User index? | Barrier present? |
       |------|----------|------------|-----------------|
       | capabilities.rs | entry_ref | yes | yes |
       | ... |

       ## Open Bugs / Known Risks

       | ID | File | Description | Status |
       |----|------|-------------|--------|
       | (none or list) |

       ## Verification

       `scripts/verify` passes: yes/no
       All tests pass: yes/no
       ```

    4. Verify the report is accurate by running `scripts/verify` one final time and confirming the gap counts match.
  </action>
  <verify>
    <automated>cd /Users/user/Sites/kernel && scripts/verify && echo "VERIFY PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-unsafe-audit/AUDIT-REPORT.md` exists
    - The report lists every .rs file in frame/ with a status
    - The gap column shows 0 for every file (or documents why not)
    - `scripts/verify` passes
    - The report accurately reflects the current codebase state (not stale data from the context file)
  </acceptance_criteria>
  <done>AUDIT-REPORT.md exists with per-file status for every file in frame/. All files show clean or fixed status. Gap count is 0. Any known risks are documented.</done>
</task>

</tasks>

<verification>
- `.planning/phases/02-unsafe-audit/AUDIT-REPORT.md` exists and is complete
- Every file in `src/frame/**/*.rs` appears in the report
- `scripts/verify` passes
</verification>

<success_criteria>
1. AUDIT-REPORT.md exists with per-file status (clean / fixed / known-risk)
2. Every file in frame/ is covered
3. Summary statistics are accurate and match codebase state
4. Any open bugs are documented with status
5. scripts/verify passes as final gate
</success_criteria>

<output>
After completion, create `.planning/phases/02-unsafe-audit/02-04-SUMMARY.md`
</output>
