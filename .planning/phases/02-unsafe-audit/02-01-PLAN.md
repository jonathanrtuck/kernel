---
phase: 02-unsafe-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frame/cores.rs
  - src/frame/fields.rs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03]

must_haves:
  truths:
    - "Every unsafe block in cores.rs has a SAFETY comment"
    - "Every unsafe block in fields.rs has a SAFETY comment"
    - "Every SAFETY comment's stated precondition is confirmed against all call sites"
    - "No aliasing violations exist in cores.rs or fields.rs"
  artifacts:
    - path: "src/frame/cores.rs"
      provides: "Audited hot-path dispatch unsafe operations"
      contains: "// SAFETY:"
    - path: "src/frame/fields.rs"
      provides: "Audited Field unsafe operations"
      contains: "// SAFETY:"
  key_links:
    - from: "src/frame/cores.rs"
      to: "src/core_manager.rs"
      via: "Observer pointer dereferences"
      pattern: "observer_ptr\\.as_ref\\(\\)|observer_ptr\\.as_ptr\\(\\)"
    - from: "src/frame/fields.rs"
      to: "src/field.rs"
      via: "Queue/waiter/routing pointer operations"
      pattern: "queue\\.as_ptr\\(\\)|entry\\.as_ptr\\(\\)"
---

<objective>
Audit every unsafe block in cores.rs (81 unsafe, 5 gap) and fields.rs (46 unsafe, -6 gap but needs accuracy audit).

Purpose: These are the two largest files in frame/ with 127 unsafe blocks combined. cores.rs is the syscall dispatch hot path; fields.rs is the IPC message queue and intrusive linked list layer. Both are critical for kernel correctness.

Output: Both files have zero SAFETY comment gaps, all existing SAFETY comments are verified accurate against callers, and no aliasing violations exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/frame/cores.rs
@src/frame/fields.rs
@src/core_manager.rs
@src/field.rs
@src/observer.rs
@src/capability.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit cores.rs — close SAFETY gaps, verify preconditions, check aliasing</name>
  <files>src/frame/cores.rs</files>
  <read_first>
    src/frame/cores.rs
    src/core_manager.rs (callers of cores.rs functions)
    src/observer.rs (Observer struct, PrimaryState, WaitState)
    src/capability.rs (Entry, Table, TransferredCap)
    CLAUDE.md (unsafe rules, SAFETY comment format)
  </read_first>
  <action>
    Systematic audit of all ~81 unsafe blocks in cores.rs:

    1. SAFETY COMMENT GAP CLOSURE (SAFE-01): The file has ~76 SAFETY comments for ~81 unsafe blocks. Find the 5 missing SAFETY comments. For each unsafe block WITHOUT a preceding `// SAFETY:` comment, add one that explains:
       - What invariant the block relies on
       - What would break if the invariant were violated
       Known gap locations to check carefully: test-only functions (alloc_test_register_state, write_test_ipc_registers, write_test_typed_registers, read_ipc_carry_and_x0) and test module unsafe blocks — these may have shortened or missing comments.

    2. PRECONDITION VERIFICATION (SAFE-02): For each SAFETY comment that says "observer_ptr points to a live Observer":
       - Trace the observer_ptr back to its source: CoreState::current, Arena::get_mut, or NonNull::from in tests
       - Verify the source actually guarantees liveness
       - Verify "A4 non-reentrancy" claim is correct for each call site
       - Check that no function uses observer_ptr after the Observer could have been freed

    3. ALIASING CHECK (SAFE-03): Focus on these high-risk patterns:
       - `observer_prepare_wait` and `observer_prepare_pending`: both create `&mut *observer_ptr.as_ptr()` and then return a `&'static mut WaitEntry` from inside. Verify no caller holds both a &mut Observer and the returned &mut WaitEntry simultaneously.
       - `call_reply_recv`: takes two `NonNull<Field>` and creates two `&mut Field`. Verify the caller always checks they are different ObjectIds.
       - `observer_extend_cap_table`: takes `&mut *observer_ptr.as_ptr()` while also receiving `new_entries` that was just initialized. Verify no aliasing between old and new entry arrays.
       - Register state access pattern: many functions cast `observer.register_state.as_ptr().as_ptr()` to `*mut RegisterState`. Verify no two simultaneous mutable casts to the same RegisterState.

    4. For every fix, ensure the SAFETY comment follows the project format: `// SAFETY: <invariant description>` on the line immediately before the `unsafe` block.

    Do NOT change any logic or functional behavior. This is a comment-only audit (adding or correcting SAFETY comments). If a genuine soundness bug is found, document it with `// BUG:` and `Status: open-bug` but do NOT attempt to fix it in this task.
  </action>
  <verify>
    <automated>cd /Users/user/Sites/kernel && scripts/verify</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '// SAFETY:' src/frame/cores.rs` equals the number of `unsafe {` blocks (currently 81 unsafe blocks; after audit every one has a SAFETY comment, count should be >= 81)
    - `grep -c 'unsafe {' src/frame/cores.rs` == `grep -c '// SAFETY:' src/frame/cores.rs` (1:1 correspondence)
    - `scripts/verify` passes (no clippy errors, all tests pass)
    - Zero `unsafe {` blocks exist without a preceding `// SAFETY:` comment
  </acceptance_criteria>
  <done>Every unsafe block in cores.rs has a SAFETY comment. All SAFETY preconditions are verified against callers. No aliasing violations found (or any found are documented as open-bug).</done>
</task>

<task type="auto">
  <name>Task 2: Audit fields.rs — verify SAFETY accuracy, close gaps, check aliasing</name>
  <files>src/frame/fields.rs</files>
  <read_first>
    src/frame/fields.rs
    src/field.rs (callers of fields.rs functions — enqueue/dequeue/add_waiter/etc.)
    src/communication.rs (IPC send/receive paths that call waiter_pop_front, waiter_observer)
    src/observer.rs (WaitEntry struct definition)
    CLAUDE.md (unsafe rules)
  </read_first>
  <action>
    Systematic audit of all ~46 unsafe blocks in fields.rs:

    1. SAFETY COMMENT ACCURACY (SAFE-01, SAFE-02): fields.rs has more SAFETY comments (52) than unsafe blocks (46) — some multi-line blocks or nested blocks have extra comments. For each unsafe block:
       - Verify the SAFETY comment accurately describes the current invariant
       - If a SAFETY comment references a function name or caller pattern that has changed since it was written, update it
       - Ensure 1:1 mapping: every `unsafe {` has exactly one `// SAFETY:` immediately before it

    2. PRECONDITION VERIFICATION (SAFE-02): Focus on pointer validity claims:
       - `queue_write`/`queue_read`: verify "Field construction guarantees `queue` points to a valid contiguous array" — trace back to allocate_field_queue and alloc_test_queue
       - `waiter_push_back`/`waiter_remove`/`waiter_pop_front`: verify "tail_ptr was set by a prior push_back" — trace the list lifecycle
       - `route_lookup`/`route_add`: verify "table_ptr was set by route_add" — trace routing table lifecycle
       - `badge_map_increment`/`badge_map_decrement`/`drain_pending_closures`: verify "map_ptr was returned by allocate_badge_map" — trace badge map lifecycle
       - Bare-metal allocation functions (`allocate_routing_table`, `allocate_badge_map`, `grow_routing_table`, `grow_badge_map`): verify PA-to-VA conversion safety and exclusive ownership claims

    3. ALIASING CHECK (SAFE-03):
       - `waiter_push_back`: creates `tail_ptr.as_mut().next` while `entry` is also live — verify no overlap
       - `waiter_remove`: creates `prev_ptr.as_mut()` and `next_ptr.as_mut()` — verify these are always distinct from `entry`
       - `route_add` shifting logic: uses `core::ptr::copy` to shift entries — verify source and destination can overlap (memmove, not memcpy)
       - `badge_map_decrement` swap-remove: uses `copy_nonoverlapping` — verify `i != last_idx` guard prevents self-copy
       - `drain_pending_closures` swap-remove: same pattern — verify guard

    4. For every fix, ensure the SAFETY comment format matches: `// SAFETY: <invariant description>`.

    Do NOT change any logic. Comment-only audit. Document any genuine soundness bugs with `// BUG:` and `Status: open-bug`.
  </action>
  <verify>
    <automated>cd /Users/user/Sites/kernel && scripts/verify</automated>
  </verify>
  <acceptance_criteria>
    - Every `unsafe {` block in fields.rs has exactly one preceding `// SAFETY:` comment
    - `scripts/verify` passes
    - No `unsafe {` block exists without a `// SAFETY:` comment
    - If SAFETY comments were inaccurate, they are corrected (not just present)
  </acceptance_criteria>
  <done>Every unsafe block in fields.rs has an accurate SAFETY comment. All preconditions verified against callers. No aliasing violations found (or documented as open-bug).</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -rn 'unsafe {' src/frame/cores.rs src/frame/fields.rs | wc -l` matches `grep -rn '// SAFETY:' src/frame/cores.rs src/frame/fields.rs | wc -l`
- `scripts/verify` passes
- No logic changes — only SAFETY comment additions/corrections
</verification>

<success_criteria>
1. Zero SAFETY comment gaps in cores.rs and fields.rs combined
2. Every SAFETY comment's precondition is verified accurate against callers
3. No aliasing violations discovered, or any found are documented as open-bug
4. scripts/verify passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-unsafe-audit/02-01-SUMMARY.md`
</output>
